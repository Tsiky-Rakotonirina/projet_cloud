services:
  db:
    image: postgis/postgis:16-3.5
    container_name: bdd
    restart: unless-stopped
    environment:
      POSTGRES_DB: route
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./database/table.sql:/docker-entrypoint-initdb.d/01_table.sql
      - ./database/data.sql:/docker-entrypoint-initdb.d/02_data.sql
      - ./database/sync_tables.sql:/docker-entrypoint-initdb.d/03_sync_tables.sql

  API:
    image: node:20-alpine
    container_name: API
    working_dir: /app
    volumes:
      - ./API:/app
      - /app/node_modules
      - uploads-data:/app/uploads
    command: sh -c "npm install && npm run dev"
    ports:
      - "3000:3000"
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: route
      DB_USER: admin
      DB_PASSWORD: admin
      JWT_SECRET: lalan_tsara_secret_key_2026
      JWT_EXPIRES_IN: 24h
      MAX_LOGIN_ATTEMPTS: 5
      LOGIN_LOCK_DURATION: 120000
    depends_on:
      - db

  # ============================================
  # SERVEUR DE TUILES OFFLINE - Antananarivo
  # ============================================
  tileserver:
    build:
      context: ./tiles
      dockerfile: Dockerfile
    container_name: tileserver
    restart: unless-stopped
    volumes:
      - tiles-cache:/app/cache
    ports:
      - "3001:8080"
    environment:
      - CACHE_DIR=/app/cache
      - DEFAULT_CENTER_LAT=-18.8792
      - DEFAULT_CENTER_LON=47.5079
      - DEFAULT_ZOOM=12

  # ============================================
  # APPLICATION WEB
  # ============================================
  web:
    image: node:20-alpine
    container_name: web
    working_dir: /app
    volumes:
      - ./web:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:3000
      - VITE_TILE_SERVER_URL=http://localhost:3001
    depends_on:
      - API
      - tileserver

volumes:
  pgdata:
  tiles-cache:
  uploads-data:
